default_hosts: &default_host
  hosts:
    - mongo:27017

production:
  clients:
    default:
      uri: <%= ENV['MONGOLAB_URI'] %>

development:
  clients:
    default:
      database: dharma_api_dev
#      <<: *default_host
      hosts:
      - mongo:27017

test:
  clients:
    default:
      database: dharma_api_test
#      <<: *default_host
      hosts:
      - mongo:27017
